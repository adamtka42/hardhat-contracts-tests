// SPDX-License-Identifier: MIT
pragma hyperion >=0.0;

contract CallContextMock {
    function observe() public payable returns (uint256, uint256, uint256, uint256, uint256) {
        return (msg.value, address(this).balance, block.chainid, block.basefee, tx.gasprice);
    }
}
